import { KnownErrors } from "../index.js";

//#region src/helpers/password.ts
const minLength = 8;
const maxLength = 70;
function getPasswordError(password) {
	if (password.length < minLength) return new KnownErrors.PasswordTooShort(minLength);
	if (password.length > maxLength) return new KnownErrors.PasswordTooLong(maxLength);
}

//#endregion
export { getPasswordError };
//# sourceMappingURL=password.js.map