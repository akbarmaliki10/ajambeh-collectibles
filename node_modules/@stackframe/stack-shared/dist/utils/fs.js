Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_chunk = require('../chunk-BE-pF4vm.js');
let path = require("path");
path = require_chunk.__toESM(path);
let fs = require("fs");
fs = require_chunk.__toESM(fs);

//#region src/utils/fs.tsx
async function list(path$1) {
	return await fs.promises.readdir(path$1);
}
async function listRecursively(p, options = {}) {
	const files = await list(p);
	return [...(await Promise.all(files.map(async (fileName) => {
		const filePath = path.join(p, fileName);
		if ((await fs.promises.stat(filePath)).isDirectory()) return [...await listRecursively(filePath, options), ...options.excludeDirectories ? [] : [filePath]];
		else return [filePath];
	}))).flat()];
}
function writeFileSyncIfChanged(path$2, content) {
	if (fs.existsSync(path$2)) {
		if (fs.readFileSync(path$2, "utf-8") === content) return;
	}
	fs.writeFileSync(path$2, content);
}

//#endregion
exports.list = list;
exports.listRecursively = listRecursively;
exports.writeFileSyncIfChanged = writeFileSyncIfChanged;
//# sourceMappingURL=fs.js.map