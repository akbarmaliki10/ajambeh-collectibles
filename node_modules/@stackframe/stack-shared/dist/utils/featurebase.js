Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_chunk = require('../chunk-BE-pF4vm.js');
let __errors_js = require("./errors.js");
let __env_js = require("./env.js");

//#region src/utils/featurebase.tsx
/**
* Find a Featurebase user by their Stack Auth user ID
*/
async function findFeaturebaseUserById(stackAuthUserId, apiKey) {
	try {
		const response = await fetch(`https://do.featurebase.app/v2/organization/identifyUser?id=${stackAuthUserId}`, {
			method: "GET",
			headers: { "X-API-Key": apiKey }
		});
		if (response.status === 404) return null;
		if (!response.ok) throw new __errors_js.StackAssertionError(`Failed to find Featurebase user by ID: ${response.statusText}`);
		const data = await response.json();
		const user = data.user;
		if (!user) throw new __errors_js.StackAssertionError(`Featurebase API returned success but no user data for ID: ${stackAuthUserId}`, { data });
		return {
			userId: user.externalUserId || user.userId || stackAuthUserId,
			email: user.email,
			name: user.name,
			profilePicture: user.profilePicture
		};
	} catch (error) {
		if (error instanceof __errors_js.StackAssertionError) throw error;
		throw new __errors_js.StackAssertionError("Failed to find Featurebase user by ID", { cause: error });
	}
}
/**
* Find a Featurebase user by their email address
*/
async function findFeaturebaseUserByEmail(email, apiKey) {
	try {
		const response = await fetch(`https://do.featurebase.app/v2/organization/identifyUser?email=${encodeURIComponent(email)}`, {
			method: "GET",
			headers: { "X-API-Key": apiKey }
		});
		if (response.status === 404) return null;
		if (!response.ok) throw new __errors_js.StackAssertionError(`Failed to find Featurebase user by email: ${response.statusText}`);
		const data = await response.json();
		const user = data.user;
		if (!user) throw new __errors_js.StackAssertionError(`Featurebase API returned success but no user data for email: ${email}`, { data });
		return {
			userId: user.externalUserId || user.userId,
			email: user.email,
			name: user.name,
			profilePicture: user.profilePicture
		};
	} catch (error) {
		console.error("Error finding Featurebase user by email:", error);
		return null;
	}
}
/**
* Create a new Featurebase user using the identifyUser endpoint
*/
async function createFeaturebaseUser(user, apiKey) {
	try {
		const response = await fetch("https://do.featurebase.app/v2/organization/identifyUser", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
				"X-API-Key": apiKey
			},
			body: JSON.stringify({
				userId: user.userId,
				email: user.email,
				name: user.name,
				profilePicture: user.profilePicture
			})
		});
		if (!response.ok) {
			const errorData = await response.json().catch(() => ({}));
			throw new __errors_js.StackAssertionError(`Failed to create Featurebase user: ${errorData.error || response.statusText}`, { errorData });
		}
		return user;
	} catch (error) {
		if (error instanceof __errors_js.StackAssertionError) throw error;
		throw new __errors_js.StackAssertionError("Failed to create Featurebase user", { cause: error });
	}
}
/**
* Update an existing Featurebase user (excluding email)
*/
async function updateFeaturebaseUser(userId, updates, apiKey) {
	try {
		const response = await fetch(`https://do.featurebase.app/v2/users/${userId}`, {
			method: "PATCH",
			headers: {
				"Content-Type": "application/json",
				"X-API-Key": apiKey
			},
			body: JSON.stringify(updates)
		});
		if (!response.ok) {
			const errorData = await response.json().catch(() => ({}));
			throw new __errors_js.StackAssertionError(`Failed to update Featurebase user: ${errorData.error || response.statusText}`, { errorData });
		}
		const data = await response.json();
		return {
			userId: data.userId || userId,
			email: data.email,
			name: data.name,
			profilePicture: data.profilePicture
		};
	} catch (error) {
		if (error instanceof __errors_js.StackAssertionError) throw error;
		throw new __errors_js.StackAssertionError("Failed to update Featurebase user", { cause: error });
	}
}
/**
* Get or create a Featurebase user based on Stack Auth user data.
* This function ensures that:
* 1. We never change a user's email address on Featurebase
* 2. We use Stack Auth user ID as the primary identifier
* 3. We handle email conflicts by using fallback emails
* 4. We update profile information when needed
*/
async function getOrCreateFeaturebaseUser(stackAuthUser, options) {
	const apiKey = options?.apiKey || (0, __env_js.getEnvVariable)("STACK_FEATUREBASE_API_KEY");
	const fallbackEmail = `${stackAuthUser.id}@featurebase-user.stack-auth-app.com`;
	const existingById = await findFeaturebaseUserById(stackAuthUser.id, apiKey);
	if (existingById) {
		let ensuredEmail = existingById.email;
		if (!ensuredEmail) try {
			await createFeaturebaseUser({
				userId: existingById.userId,
				email: fallbackEmail,
				name: stackAuthUser.displayName || void 0,
				profilePicture: stackAuthUser.profileImageUrl || void 0
			}, apiKey);
			ensuredEmail = fallbackEmail;
		} catch (e) {
			throw new __errors_js.StackAssertionError(`Failed to set fallback email for existing Featurebase user ${existingById.userId}`, { cause: e });
		}
		try {
			const updates = {};
			if (stackAuthUser.displayName && stackAuthUser.displayName !== existingById.name) updates.name = stackAuthUser.displayName;
			if (stackAuthUser.profileImageUrl && stackAuthUser.profileImageUrl !== existingById.profilePicture) updates.profilePicture = stackAuthUser.profileImageUrl;
			if (Object.keys(updates).length > 0) await updateFeaturebaseUser(existingById.userId, updates, apiKey);
		} catch (error) {
			console.error("Failed to update existing Featurebase user profile:", error);
		}
		return {
			userId: existingById.userId,
			email: ensuredEmail
		};
	}
	const candidateEmail = stackAuthUser.primaryEmail ?? fallbackEmail;
	const safeEmail = await findFeaturebaseUserByEmail(candidateEmail, apiKey) ? fallbackEmail : candidateEmail;
	const created = await createFeaturebaseUser({
		userId: stackAuthUser.id,
		email: safeEmail,
		name: stackAuthUser.displayName || stackAuthUser.primaryEmail?.split("@")[0] || "User",
		profilePicture: stackAuthUser.profileImageUrl || void 0
	}, apiKey);
	return {
		userId: created.userId,
		email: created.email
	};
}

//#endregion
exports.getOrCreateFeaturebaseUser = getOrCreateFeaturebaseUser;
//# sourceMappingURL=featurebase.js.map