"use client";

import { DownloadIcon } from "lucide-react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { Cross2Icon } from "@radix-ui/react-icons";
import { Button } from "../ui/button.js";
import { DataTableViewOptions } from "./view-options.js";
import { deepPlainEquals } from "@stackframe/stack-shared/dist/utils/objects";
import { download, generateCsv, mkConfig } from "export-to-csv";

//#region src/components/data-table/toolbar.tsx
function DataTableToolbar({ table, toolbarRender, showDefaultToolbar, defaultColumnFilters, defaultSorting, showResetFilters = true }) {
	const isFiltered = !deepPlainEquals(table.getState().columnFilters, defaultColumnFilters);
	const isSorted = !deepPlainEquals(table.getState().sorting, defaultSorting);
	return /* @__PURE__ */ jsxs("div", {
		className: "flex items-center justify-between",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "flex items-center gap-2 flex-wrap flex-1 stack-scope",
			children: [toolbarRender?.(table), showResetFilters && (isFiltered || isSorted) && /* @__PURE__ */ jsxs(Button, {
				variant: "ghost",
				onClick: () => {
					table.setColumnFilters(defaultColumnFilters);
					table.setSorting(defaultSorting);
				},
				className: "h-8 px-2 lg:px-3",
				children: ["Reset filters", /* @__PURE__ */ jsx(Cross2Icon, { className: "ml-2 h-4 w-4" })]
			})]
		}), showDefaultToolbar && /* @__PURE__ */ jsx(Fragment, { children: /* @__PURE__ */ jsxs("div", {
			className: "flex items-center gap-2 flex-wrap",
			children: [/* @__PURE__ */ jsx(DataTableViewOptions, { table }), /* @__PURE__ */ jsxs(Button, {
				variant: "outline",
				size: "sm",
				className: "ml-auto hidden h-8 lg:flex",
				onClick: () => {
					const csvConfig = mkConfig({
						fieldSeparator: ",",
						filename: "data",
						decimalSeparator: ".",
						useKeysAsHeaders: true
					});
					const renderCellValue = (cell) => {
						const rendered = cell.renderValue();
						if (rendered === null) return;
						if ([
							"string",
							"number",
							"boolean",
							"undefined"
						].includes(typeof rendered)) return rendered;
						if (rendered instanceof Date) return rendered.toISOString();
						if (typeof rendered === "object") return JSON.stringify(rendered);
					};
					const rows = table.getCoreRowModel().rows.map((row) => Object.fromEntries(row.getAllCells().map((c) => [c.column.id, renderCellValue(c)]).filter(([_, v]) => v !== void 0)));
					if (rows.length === 0) {
						alert("No data to export");
						return;
					}
					const csv = generateCsv(csvConfig)(rows);
					download(csvConfig)(csv);
				},
				children: [/* @__PURE__ */ jsx(DownloadIcon, { className: "mr-2 h-4 w-4" }), "Export CSV"]
			})]
		}) })]
	});
}

//#endregion
export { DataTableToolbar };
//# sourceMappingURL=toolbar.js.map