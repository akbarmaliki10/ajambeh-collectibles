"use client";

import React, { useId } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { SsrScript } from "./ssr-layout-effect.js";

//#region src/components/elements/maybe-full-page.tsx
function MaybeFullPage({ children, fullPage }) {
	const id = `stack-full-page-container-${useId()}`;
	const scriptString = `(([id]) => {
    const el = document.getElementById(id);
    if (!el) {
      // component is not full page
      return;
    }
    const offset = el.getBoundingClientRect().top + document.documentElement.scrollTop;
    el.style.minHeight = \`calc(100vh - \${offset}px)\`;
  })(${JSON.stringify([id])})`;
	if (fullPage) return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
		suppressHydrationWarning: true,
		id,
		style: {
			minHeight: "100vh",
			alignSelf: "stretch",
			flexGrow: 1,
			display: "flex",
			justifyContent: "center",
			alignItems: "center"
		},
		className: "stack-scope",
		children
	}), /* @__PURE__ */ jsx(SsrScript, { script: scriptString })] });
	else return /* @__PURE__ */ jsx(Fragment, { children });
}

//#endregion
export { MaybeFullPage };
//# sourceMappingURL=maybe-full-page.js.map